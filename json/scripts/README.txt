Por cada script que hay en esta carpeta se hará lo siguiente:

    * Se buscará el script en la app de Corus. Reemplazando "__" por "/" y "___" por "/:"

    * En caso de que ya exista el script se actualizará (si el usuario confirma)

    * En caso de que no exista el script se creará
